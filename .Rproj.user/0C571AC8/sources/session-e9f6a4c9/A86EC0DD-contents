# Dataframs are much like dictionaries in python 
# dicts in python get's converted into dataframes 
# through pands anyway .....

df <- data.frame(cloumn1 = c(1L, 2L, 3L), column2 = c(2.5, 6.5, 3.5))

print(df)


# Using DNase data in R

library("tidyverse")

data("DNase") # Get's used to read datasets 

View(DNase) 

# Viewing the head area of the dataset DNase

head(DNase)

str(DNase) # Gives the structure of the data

colnames(DNase) # Gives only the column names of a dataset

mutate(DNase, conc = conc*100) # To mutate a coulmn of dataset


data("mtcars")

mtcars



